server {
    listen 80;
    root /var/www;
    
    location / {
        gzip_static on;
        expires max;
        add_header Cache-Control public;
        sendfile on;
        sendfile_max_chunk 250k;
	    try_files $uri =404;
        limit_rate_after 2000k;
        limit_rate 1000k;
    }
}
